# === MIRAJ PORTFOLIO - Environment Variables Template ===
# 
# INSTRUCTIONS:
# 1. Copy this file to .env.local for development
# 2. Replace REPLACE_ME values with actual credentials
# 3. NEVER commit .env.local or any .env* files
#
# For production deployment (Vercel/Netlify):
# Set these variables in your deployment platform's environment settings

# === PUBLIC VARIABLES (exposed to browser) ===
# These are safe to commit if needed, but better to keep in .env.local
NEXT_PUBLIC_SITE_URL=https://your-portfolio.vercel.app
NEXT_PUBLIC_SITE_NAME="Miraj Prasain Portfolio"

# === CONTACT FORM (if you add contact functionality) ===
# EmailJS credentials (if using EmailJS)
# NEXT_PUBLIC_EMAILJS_SERVICE_ID=REPLACE_ME
# NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=REPLACE_ME  
# NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=REPLACE_ME

# SendGrid API (if using SendGrid)
# SENDGRID_API_KEY=REPLACE_ME
# SENDGRID_FROM_EMAIL=your-email@domain.com

# === ANALYTICS (optional) ===
# Google Analytics
# NEXT_PUBLIC_GA_MEASUREMENT_ID=REPLACE_ME

# Umami Analytics (privacy-focused alternative)
# NEXT_PUBLIC_UMAMI_WEBSITE_ID=REPLACE_ME
# NEXT_PUBLIC_UMAMI_URL=REPLACE_ME

# === CMS/CONTENT (if you add a headless CMS) ===
# Contentful
# CONTENTFUL_SPACE_ID=REPLACE_ME
# CONTENTFUL_ACCESS_TOKEN=REPLACE_ME

# Sanity
# NEXT_PUBLIC_SANITY_PROJECT_ID=REPLACE_ME
# NEXT_PUBLIC_SANITY_DATASET=production
# SANITY_API_TOKEN=REPLACE_ME

# === DATABASE (if you add backend features) ===
# MongoDB Atlas
# MONGODB_URI=REPLACE_ME

# PostgreSQL (Vercel Postgres, Supabase, etc.)
# DATABASE_URL=REPLACE_ME

# === AUTHENTICATION (if you add admin features) ===
# NextAuth.js
# NEXTAUTH_URL=http://localhost:3000
# NEXTAUTH_SECRET=REPLACE_ME
# NEXTAUTH_URL_INTERNAL=http://localhost:3000

# GitHub OAuth (if admin login needed)
# GITHUB_CLIENT_ID=REPLACE_ME
# GITHUB_CLIENT_SECRET=REPLACE_ME

# === API INTEGRATIONS ===
# GitHub API (for fetching repos dynamically)
# GITHUB_TOKEN=REPLACE_ME

# === DEVELOPMENT ONLY ===
# Node environment
NODE_ENV=development